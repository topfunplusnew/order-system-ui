## 1. API and configuration

- [x] 1.1 Add failing tests for the oil card ledger API paths and methods.
- [x] 1.2 Add the oil card ledger API module.
- [x] 1.3 Add failing tests for main/sub columns, query construction, payload whitelisting, export filters, and delete ID serialization.
- [x] 1.4 Add the shared oil card ledger configuration helpers.

## 2. Shared page and entries

- [x] 2.1 Add failing source-contract tests for the two entry components and shared component behavior.
- [x] 2.2 Add the shared oil card ledger list and form component.
- [x] 2.3 Add the main-card entry component with main-card permissions and configuration.
- [x] 2.4 Add the sub-card entry component with sub-card permissions and configuration.

## 3. Verification

- [x] 3.1 Run focused Jest tests.
- [x] 3.2 Run targeted ESLint checks.
- [x] 3.3 Run the staging build and inspect the final diff.
