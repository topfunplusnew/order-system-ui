# UploadFilesButton 组件附件显示功能修改说明

## 修改概述

为了支持在编辑收款和付款信息时显示已有附件，对以下文件进行了修改：

## 修改的文件

### 1. UploadFilesButton 组件 (`src/components/UploadFilesButton/index.vue`)

**新增功能：**
- 添加了 `initialAttachments` prop，用于接收初始附件列表
- 添加了 `initializeAttachments` 方法，用于初始化附件列表
- 添加了 `setAttachments` 方法，用于手动设置附件列表
- 在 `watch` 中监听 `initialAttachments` 变化，自动初始化
- 在 `created` 钩子中初始化附件列表
- 更新了组件注释说明

**Props 说明：**
```javascript
initialAttachments: {
  type: Array,
  default: () => []
}
```

**使用示例：**
```vue
<UploadFilesButton 
  :initial-attachments="form.attachmentList || []"
  @files-updated="handleFilesUpdated"
  ref="uploadButton"
/>
```

### 2. 收款页面 (`src/views/system/receiveMoney/index.vue`)

**修改内容：**
- 在 UploadFilesButton 组件上添加了 `:initial-attachments` 属性
- 在 `cancel` 方法中添加了清除上传组件状态的逻辑
- 在 `reset` 方法中添加了清除上传组件状态的逻辑
- 移除了 `handleUpdate` 方法中手动设置附件的重复代码

**关键修改：**
```vue
<UploadFilesButton 
  ref="attachmentUploader" 
  flag="transactionHistoryAttachment"
  :extra-info="{ moduleType: 'receiveMoney', formId: form.id }"
  :initial-attachments="form.attachmentList || []"
  @files-updated="handleAttachmentFilesUpdated" 
/>
```

### 3. 付款页面 (`src/views/system/payment/index.vue`)

**修改内容：**
- 在两个 UploadFilesButton 组件上添加了 `:initial-attachments` 属性
- 更新了 `handleTransactionHistoryFilesUpdated` 方法，正确设置银行流水附件ID
- 在 form 对象的 params 中添加了 `transactionHistoryAttachmentIds` 字段
- 在 `handleEdit` 方法中添加了银行流水附件ID的初始化
- 移除了手动设置附件的重复代码

**关键修改：**
```vue
<!-- 普通附件 -->
<UploadFilesButton 
  ref="attachmentUpload" 
  flag="attachments"
  :extra-info="{ moduleType: 'payment', formId: form.id }"
  :initial-attachments="form.attachmentList || []"
  @files-updated="handleAttachmentFilesUpdated" 
/>

<!-- 银行流水附件 -->
<UploadFilesButton 
  ref="transactionHistoryUpload" 
  flag="transactionHistoryAttachmentList"
  :extra-info="{ moduleType: 'payment', formId: form.id }"
  :initial-attachments="form.transactionHistoryAttachmentList || []"
  @files-updated="handleTransactionHistoryFilesUpdated" 
/>
```

## 功能说明

### 编辑时的工作流程

1. **点击修改按钮时：**
   - 调用 API 获取详细数据（包含 `attachmentList` 字段）
   - 将附件信息设置到 form 对象中
   - UploadFilesButton 组件通过 `:initial-attachments` prop 接收附件列表
   - 组件自动初始化显示已有附件

2. **上传新附件时：**
   - 新上传的附件会添加到已有附件列表中
   - 通过 `files-updated` 事件将所有附件ID传递给父组件
   - 父组件将附件ID设置到 `form.params.attachmentIds` 中

3. **删除附件时：**
   - 可以删除已有附件或新上传的附件
   - 删除后的附件ID列表会自动更新到父组件

4. **表单提交时：**
   - `form.params.attachmentIds` 包含所有附件的ID
   - 后端接收到完整的附件ID列表进行处理

### 数据结构说明

**收款信息的附件字段：**
- `attachmentList`: 附件详细信息列表（用于显示）
- `params.attachmentIds`: 附件ID列表（用于提交）

**付款信息的附件字段：**
- `attachmentList`: 普通附件详细信息列表
- `transactionHistoryAttachmentList`: 银行流水附件详细信息列表
- `params.attachmentIds`: 普通附件ID列表
- `params.transactionHistoryAttachmentIds`: 银行流水附件ID列表

## 测试建议

1. **新增测试：**
   - 创建新的收款/付款信息
   - 上传附件
   - 保存后验证附件是否正确保存

2. **编辑测试：**
   - 编辑已有的收款/付款信息
   - 验证已有附件是否正确显示
   - 添加新附件，验证是否能正常上传
   - 删除已有附件，验证是否能正常删除
   - 保存后验证附件列表是否正确更新

3. **清理测试：**
   - 取消编辑，验证上传组件状态是否正确清理
   - 重新编辑，验证不会残留之前的状态

## 注意事项

1. **Vuex 状态管理：**
   - 组件使用全局 Vuex store 管理附件ID
   - 确保在组件销毁或重置时正确清理状态

2. **性能考虑：**
   - 大量附件时注意内存使用
   - 建议对附件数量进行限制

3. **错误处理：**
   - 网络错误时的重试机制
   - 文件格式/大小限制的提示

4. **兼容性：**
   - 确保与现有的 mixin_checkfile 兼容
   - 保持与后端接口的数据格式一致
