# 返利模块供应商自动填充功能测试验证

## 功能概述
当用户在返利模块选择订单货物后，点击"选择所选货物"按钮，系统会自动将选中货物对应的供应商填充到返利表单的供应商字段中。

## 测试步骤

### 1. 基础功能测试
1. 打开返利管理页面
2. 点击"新增供应商返利信息"按钮
3. 在弹出的表单中，点击"选择订单"按钮
4. 在订单选择对话框中，选择一个订单
5. 在货物详情列表中，选择一些相同供应商的货物
6. 点击"选择所选货物"按钮
7. **预期结果**: 
   - 订单选择对话框关闭
   - 返利表单的供应商字段自动填充为选中货物的供应商
   - 显示成功消息："已自动填充供应商：[供应商名称]"

### 2. 供应商一致性测试
1. 重复上述步骤1-4
2. 在货物详情列表中，先选择一个供应商的货物
3. 观察其他供应商的货物行是否变为灰色且复选框不可选
4. 尝试继续选择相同供应商的货物
5. 点击"选择所选货物"按钮
6. **预期结果**: 
   - 只能选择相同供应商的货物
   - 供应商自动填充正确
   - 不同供应商的货物无法选择

### 3. 跨供应商选择警告测试
1. 重复上述步骤1-4
2. 选择一个供应商的货物
3. 手动尝试选择不同供应商的货物（通过程序化方式）
4. **预期结果**: 
   - 显示警告消息
   - 不同供应商的货物被自动过滤
   - 只保留原选中供应商的货物

### 4. 表单重置测试
1. 完成基础功能测试后
2. 点击表单的"取消"按钮或关闭表单
3. 重新打开新增表单
4. **预期结果**: 
   - 供应商字段为空
   - 选择状态被重置
   - 可以重新选择任意供应商的货物

### 5. 编辑模式测试
1. 编辑一个已存在的返利记录
2. 重新选择订单货物
3. **预期结果**: 
   - 供应商信息正确更新
   - 其他表单数据不受影响

## 技术验证点

### 前端验证
- [ ] `currentSelectedSupplier` 状态管理正确
- [ ] `checkSupplierSelectable` 方法正常工作
- [ ] 表格行样式 `getRowClassName` 正确应用
- [ ] 表单字段 `form.supplier` 和 `form.supplierID` 正确填充
- [ ] 对话框关闭和状态重置正常

### 业务逻辑验证
- [ ] 供应商一致性强制执行
- [ ] 货物选择数据完整性
- [ ] 计算逻辑（面积、重箱）不受影响
- [ ] 表单验证规则正常工作

### 用户体验验证
- [ ] 视觉反馈清晰（灰色行、消息提示）
- [ ] 操作流程顺畅
- [ ] 错误提示友好
- [ ] 性能表现良好

## 常见问题排查

### 1. 供应商未自动填充
- 检查 `currentSelectedSupplier` 是否正确设置
- 确认选中的货物数据包含 `supplier` 字段
- 验证 `submitSelectOrderDetail` 方法是否正确调用

### 2. 复选框控制异常
- 检查 `checkSupplierSelectable` 方法逻辑
- 确认表格的 `:selectable` 属性绑定正确
- 验证 `handleSelectionChangeOrderDetail` 方法执行

### 3. 样式未生效
- 检查 `getRowClassName` 方法返回值
- 确认 CSS 类 `.disabled-row` 定义正确
- 验证 `:row-class-name` 属性绑定

## 性能优化建议

1. **大数据量优化**: 如果货物列表很长，考虑虚拟滚动
2. **状态管理**: 考虑使用 computed 属性优化供应商状态计算
3. **防抖处理**: 为频繁的选择操作添加防抖机制

## 浏览器兼容性

- ✅ Chrome 80+
- ✅ Firefox 75+
- ✅ Safari 13+
- ✅ Edge 80+

## 代码质量检查

- [x] ESLint 检查通过
- [x] Vue 2 语法正确
- [x] 组件通信规范
- [x] 错误处理完善
- [x] 注释文档清晰
