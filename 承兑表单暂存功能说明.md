<!-- 测试承兑表单暂存功能的示例用法 -->

## 功能说明

已为BankType.vue组件实现了承兑表单的暂存功能，并解决了waitForBothSelection模式下双组件共享数据的问题。

### 1. 表单数据暂存
- 当用户在承兑信息填写表单中输入数据后点击"关闭"按钮时，表单数据会自动保存到Vuex store中
- 数据暂存使用 `formId + componentId` 作为唯一标识，支持多个表单实例

### 2. 双组件共享模式 (waitForBothSelection=true)
- **问题场景**: 在record/index.vue中，内部转账使用了两个BankType组件（支出方和收入方），但它们应该共享同一个承兑表单信息
- **解决方案**: 
  - 引入 `sharedFormId` 和 `sharedComponentId` 计算属性
  - 在 `waitForBothSelection` 模式下，两个组件使用统一的标识符来共享草稿数据
  - 支出方formId: `income-${form.id}` → 共享formId: `shared-${form.id}`
  - 收入方formId: `expense-${form.id}` → 共享formId: `shared-${form.id}`
  - 两个组件都使用相同的 `sharedComponentId`: `shared-${form.id}_shared`

### 3. 表单数据恢复
- **优先级管理**: 草稿数据 > 已保存承兑信息 > 空表单
- **智能恢复**: 打开承兑信息时自动从store恢复草稿数据
- **共享恢复**: 在双组件模式下，任一组件点击"填写承兑信息"都能恢复共享的草稿数据

### 4. 按钮状态动态显示
- 默认：显示"填写承兑信息"
- 有草稿数据：显示"继续填写承兑信息"（双组件模式下状态一致）
- 有正式保存的数据：显示"修改承兑信息"

### 5. 智能状态清除机制
- **重置按钮**: 清除store中的草稿数据并重置表单
- **账户类型改变**: 
  - 单组件模式: 非承兑类型时直接清除草稿数据
  - 双组件模式: 只有当双方都不是承兑类型时才清除草稿数据
- **正式提交表单**: 清除草稿数据（因为已正式保存）
- **组件销毁**: 如果有数据则先保存草稿，然后清理

## Store扩展

### 新增状态
```javascript
acceptanceFormDrafts: {
  // 格式: { [formId_componentId]: { formData: {}, timestamp: number } }
  // 双组件共享模式示例:
  // "shared-new_shared": { formData: {...}, timestamp: 1234567890 }
}
```

### 计算属性逻辑
```javascript
// 在waitForBothSelection模式下的共享标识符生成
sharedFormId() {
  if (this.waitForBothSelection) {
    // income-123 -> shared-123, expense-123 -> shared-123
    return this.formId.replace(/^(income|expense)-/, 'shared-');
  }
  return this.formId;
},

sharedComponentId() {
  if (this.waitForBothSelection) {
    // 两个组件使用相同的标识符来共享数据
    return `${this.sharedFormId}_shared`;
  }
  return this.componentId;
}
```

## 使用示例

### 单组件模式
```vue
<BankType
  :formId="'single-form-001'"
  @updateBankAcceptance="handleBankAcceptanceUpdate"
/>
```

### 双组件共享模式 (record/index.vue)
```vue
<!-- 支出方组件 -->
<BankType
  ref="selfSelectBankType"
  :form-id="`income-${form.id || 'new'}`"
  :component-role="'source'"
  :wait-for-both-selection="true"
  :is-internal-transfer="true"
/>

<!-- 收入方组件 -->
<BankType
  ref="otherSelectBankType"
  :form-id="`expense-${form.id || 'new'}`"
  :component-role="'target'"
  :wait-for-both-selection="true"
  :is-internal-transfer="true"
/>
```

在这种配置下，两个组件会：
- 使用相同的 `sharedFormId`: `shared-${form.id || 'new'}`
- 使用相同的 `sharedComponentId`: `shared-${form.id || 'new'}_shared`
- 共享同一份承兑表单草稿数据
- 按钮状态保持一致

## 测试场景

### 1. 双组件共享测试
- 打开内部转账页面
- 选择支出方账户类型为"承兑"
- 填写承兑信息（票据号码、金额等）
- 点击"关闭"按钮
- 切换到收入方，选择账户类型为"承兑"
- 验证：收入方的"填写承兑信息"按钮显示为"继续填写承兑信息"
- 点击按钮，验证：能恢复之前填写的数据

### 2. 智能清除测试
- 在双组件模式下填写承兑信息
- 将支出方账户类型改为"银行活期存款"（收入方仍为承兑）
- 验证：草稿数据不被清除
- 将收入方账户类型也改为"银行活期存款"
- 验证：草稿数据被清除

### 3. 重置功能测试
- 填写部分承兑信息
- 点击"重置"按钮
- 验证：表单被清空且草稿数据被删除
- 再次打开承兑信息，验证：显示为空表单

## 技术要点

1. **共享标识符**: 通过统一的formId和componentId实现双组件数据共享
2. **智能清除**: 根据双方账户类型状态决定是否清除草稿数据
3. **状态同步**: 两个组件的按钮状态始终保持一致
4. **数据隔离**: 不同表单实例之间的草稿数据完全隔离
5. **向下兼容**: 单组件模式的功能完全不受影响
