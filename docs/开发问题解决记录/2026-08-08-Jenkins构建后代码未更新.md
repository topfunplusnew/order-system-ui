# Jenkins 构建后代码未更新

日期：2026-08-08

## 问题现象

Jenkins 构建成功，但部署后的代码没有更新。

## 原因

本地代码已经修改，但没有推送到远程仓库。Jenkins 构建时拉取的是远程仓库代码，因此无法构建本地尚未推送的改动。

## 解决方式

构建前确认本地改动已经提交并推送：

```bash
git status
git push
```

推送成功后，再重新触发 Jenkins 构建。

