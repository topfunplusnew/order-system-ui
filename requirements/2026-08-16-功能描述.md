# 2026-08-16 功能描述（今日 TODO）

## 总目标
- 为"票点"相关模块增加表格**行高亮/变色**功能；现有类似功能的高亮颜色过浅、难以分辨，需要更换为更明显的颜色。

## 需求清单

### 1) 票点模块表格增加行高亮功能（含换色）
- 需求描述：
  - 票点相关的几个模块（发票管理）页面，表格行增加**行高亮/变色**效果；
  - 现有类似功能（订单列表 `ElTableOrder` 点击操作列高亮该行）的高亮颜色太浅（浅黄 `#fff7e8`），不好分辨，需要**更换为更明显的颜色**。
- **核心参考实现（用户指定）**：`views/dashboard/components/goodsOrder/ElTableOrder.vue`
  - 机制：点击操作列 → `trackOrderActionRow(row)` 记录 `activeOrderActionRowId`（并 `saveUserConfig` 持久化，刷新后仍高亮）→ `getOrderRowClassName` 给匹配行加 `active-order-action-row` 类 → CSS 高亮（见下）
  - 高亮 CSS：背景 `#fff7e8` + 橙色 `#e6a23c` inset 上下边框 + 首列左侧 3px 橙色边条
  - 兼容处理：同时覆盖 `.el-table__body-wrapper`、`.el-table__fixed-body-wrapper`、固定右侧列，且与调整单行色（adjust-row-*）叠加不冲突
  - **结论：交互方式已确认为"鼠标悬浮变色"**（非复刻点击高亮）；ElTableOrder 仅作高亮样式与固定列兼容处理的参考
- 涉及页面（票点模块，待确认是否覆盖全部）：
  - `views/system/allinvoice/index.vue` — 发票总台账
  - `views/system/invoiceIn/index.vue` — 进项发票
  - `views/system/invoiceOut/index.vue` — 销项发票
  - `views/system/invoiceOther/index.vue` — 其他发票
  - `views/system/noneInvoice/InvoiceIn.vue`、`noneInvoice/InvoiceOut.vue` — 无票开票（进/销）
- 其他悬浮变色参考（颜色也偏浅，可一并参考）：
  - `views/dashboard/components/common/BatchInvoicePanel.vue` `tr:hover { background-color: rgba(64, 158, 255, 0.04); }`
  - `views/dashboard/components/common/InvoiceCompanysList.vue` 同上
  - `views/system/testing/Item.vue` `$table-row-bg-hover: #f5f7fa;`
- 换色要求：**✅ 已确认**：采用浅绿 `#e1f3d8`（或 `rgba(103, 194, 58, 0.15)`），明显可分辨。
- 风险标记：无（纯前端样式改动，不涉及后端接口；不采用点击高亮，故不涉及 `saveUserConfig` 持久化）。

## 执行顺序建议
1. ~~与用户确认：新悬浮色的色值~~（✅ 已确认：浅绿 `#e1f3d8`）、页面清单（以上 6 个票点页面，以用户最终确认为准）。
2. 统一悬浮变色样式（建议抽取公共样式/变量，避免各页面重复定义）。
3. 应用到各票点模块页面，验证悬浮效果与表格滚动/固定列/斑马纹兼容。

## 交付与验收口径
- 每个需求明确状态：`已实现` / `待后端` / `待联调` / `待确认是否需求`
  - 本条：交互方式 ✅ 已确认（悬浮变色）；色值 ✅ 已确认（浅绿 `#e1f3d8`）；页面范围默认以上 6 个票点页面 → 待用户最终确认后实施 → `已实现`
- 字段为空问题定位层级：`后端未返回` / `前端字段映射错误` / `展示格式化错误`（本条不涉及）
- 按钮缺失类问题结论：`回归缺陷` / `历史未实现`（本条不涉及）
