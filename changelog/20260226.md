# 2026-02-26 问题修复清单

> 规则（按你的要求执行）：
> - 严格按仓库现有代码修复，不“凭空”造无依据的新函数/新逻辑（需要新逻辑时，必须参考仓库已有实现并保持一致）。
> - 每完成 1 个任务：立刻在本文件把对应项标记为已完成（`[x]`）。
> - 代码里必须写注释：注释内容说明“为哪个任务做了什么修改”。
> - 修复完成后不提交代码。

## 任务列表（完成即勾选）

- [x] 1、`packages/order-system/src/views/system/inventoryMain/index.vue` 和 `packages/order-system/src/views/dashboard/components/goodsOrder/ElTableOrder.vue`：导出【库存目录】与【订单目录】的 Excel 中，数字需要为 Excel 数值类型（可直接运算）。
- [x] 2、`packages/order-system/src/views/system/goback/CustomerInfo.vue` 和 `packages/order-system/src/views/system/goback/SupplierInfo.vue`：往来明细中的【借方/贷方】三列改为保留 2 位小数展示（当前为 4 位）。
- [x] 3、`packages/order-system/src/views/system/orderfreight/index.vue`：一键付运费表格合计数值保留 2 位小数（当前为 4 位）。
- [x] 4、`packages/order-system/src/components/NeedToShow/RECEIVE_MONEY.vue` 和 `packages/order-system/src/components/NeedToShow/PAYMENT.vue`：在 `el-description` 中增加【我方银行卡号】展示。
- [x] 5、
  - `packages/order-system/src/views/system/invoiceOut/index.vue`：修复“发票卖出信息确认”无法点击问题。
  - `packages/order-system/src/views/dashboard/components/goodsOrder/Invoice.vue`（由 `packages/order-system/src/views/dashboard/mixins/order/order_Invoice.js` 打开）：补充【票点】和【票点金额】输入项，并按 `invoiceOut/index.vue` 的自动计算逻辑实现自动计算（注意 Vue 响应式）。
- [x] 6、`packages/order-system/src/views/system/Statement/components/CustomerDetail.vue` 和 `packages/order-system/src/views/system/Statement/components/SupplierDetail.vue`：点击查询打开 ORDER_DETAIL（订单详情）时，展示方式需要调整为“订单模式”展示，参考 `CustomerInfo.vue` / `SupplierInfo.vue` 的订单信息展示逻辑。
- [x] 7、批量开票功能：打开的【购买方信息】区域表格显示不全，需要增加滚动条展示（补齐 `statistics-summary` 这个 class 对应的 div/样式结构）。
- [x] 8、修复报错：`ChatForm3.vue` 渲染时引用 `numToChineseUppercase` 未定义/不是函数（见控制台报错堆栈）。
- [x] 9、`packages/order-system/src/views/system/Statement/orderfreightDetailsummary.vue`、`customerSummary.vue`、`supplierSummary.vue`：表格左侧新增一列【日期】，取顶部搜索的开始/结束时间并拼成 `YYYY-MM-DD 00:00:00至YYYY-MM-DD 23:59:59`（顶部搜索不精确到秒）。
- [x] 10、`packages/order-system/src/views/system/goback/CustomerInfo.vue`：客户明细-往来明细中，当同日同时有【订单】和【调整单】时，需要拆成两列：
   - `[订单]订单金额`
  - `[调整单]调整单金额`
  > 项目内已有区分“订单/调整单”的工具函数，修复时直接复用。

## 备注

- 本文件只记录“要修什么 + 是否已完成”，每条任务的具体代码改动以对应文件中的注释为准。
  
