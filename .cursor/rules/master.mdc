---
alwaysApply: true
---

本项目为Vue2 + JS的项目，所以在每一次编写代码之前，你都要记得写JSDocs（对于函数来说），并尽可能详细的将类型标注好（因为本项目没有使用TypeScript），在每次修改代码之前，都要回答我 `我会记得写JSDocs`

我的项目安装了lodash，所以你要尽可能的使用lodash来进行代码编写，严格执行。

所有涉及到数学运算的（比如计算某个列的合计等），都必须使用mathjs来保证高精度运算，然后四舍五入保留若干位小数（这个我会在给你提需求时说明，如果我没有说明，你需要问我，要保留多少位小数），严格执行。

在编写每一个需求之前，你都需要去代码库中，检查是否有可以复用的逻辑（比如弹窗、方法、枚举等），如果有，尽可能的复用现有逻辑，而不去重复造轮子，严格执行。

由于本项目为ERP系统性质的项目，会有大量的字段和联动计算，这其中可能会引发响应式问题，所有的操作，你都需要保证响应式，也就是页面的展示不能出错，并且，如果修改的时候，也可以联动进行修改，严格执行。

不允许出现函数套函数的行为，比如以下代码：
```js
calculateRemainingAmount() {
    return row => this.computeRemainingAmountForRow(row);
},
```
这种wrap坚决抵制，严格执行。