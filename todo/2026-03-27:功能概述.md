# 2026-03-27 功能概述（今日 TODO）

## 总目标
- 整理并推进一批报表/明细页面的字段补齐、列调整、导出能力补充与异常数据排查。
- 对“后端可能未提供字段/接口”的项进行显式标记，避免前端误改。

## 需求清单

### 1) 入库记录 / 出库记录字段与列位调整
- 入库记录新增列：`入库库房`
  - 字段来源：入库管理的仓库名称。
  - 风险标记：**后端可能尚未返回该字段**，需要先确认接口返回结构。
- 出库记录列调整：将 `客户/库房` 列移动到 `车号` 列前面。
- 出库记录新增列：`出库库房`。
- 出库记录新增搜索项：`出库库房`。
  - 风险标记：**出库库房字段后端可能尚未提供**，需接口联调确认。

### 2) 导出按钮补充（疑似后端未实现）
- `资金变动详情` 页面新增导出按钮（位置：搜索区域旁）。
- `资金总额` 页面新增导出按钮（位置：搜索区域旁）。
- `本日应当利润` 页面新增导出按钮（位置：搜索区域旁）。
- 风险标记：以上导出能力 **后端可能未提供导出接口**，需先核对接口与权限。

### 3) 导出按钮缺失问题（Bug 或未实现）
- `油卡消费充值明细表`：导出按钮缺失（需确认是回归 bug 还是未实现）。
- `现金加油台账`：导出按钮缺失（需确认是回归 bug 还是未实现）。

### 4) 运费当日业务发生统计
- 问题1：`车队` 列无数据。
- 问题2：导出功能缺失。
- 要求：**先调查现状再开发**，明确是前端映射问题、后端未返回，还是权限/接口未接入。

### 5) 运费科目明细账
- 列名调整：`司机名称` 改为 `车牌号`。
- 数据问题：`收款户名账号` 列取值为空（页面无显示）。
  - 需核对：接口字段名、前端映射字段、是否存在格式化逻辑覆盖。

## 执行顺序建议
1. 先做接口与返回字段盘点（重点：库房字段、车队字段、收款户名账号、各页面导出接口）。
2. 再做纯前端可落地项（列顺序、列名调整、按钮显隐与位置）。
3. 最后处理依赖后端的功能（导出接口、新增字段展示）并记录联调结果。

## 交付与验收口径
- 每个页面明确以下状态：`已实现` / `待后端` / `待联调` / `待确认是否需求`。
- 对“导出按钮缺失”类问题，必须给出结论：`回归缺陷` 或 `历史未实现`。
- 涉及字段为空的问题，必须给出定位层级：`后端未返回` / `前端字段映射错误` / `展示格式化错误`。

## Debug（2026-03-27，基于 chrome-devtools 实测）

### 一、确认“真实缺失”的项（后端或接口能力未满足）
- `资金变动详情` 页面未发现导出按钮，且仅见查询接口 `GET /dev-api/statistics/getallmoney`，未观察到导出接口调用。
- `资金总额` 页面未发现导出按钮，且仅见查询接口 `GET /dev-api/statistics/getallmoney`，未观察到导出接口调用。
- `本日应当利润` 页面未发现导出按钮，仅见查询接口 `GET /dev-api/statistics/getDailyProfitStatistics`，未观察到导出接口调用。
  - 结论：以上 3 页“导出能力”当前可按“待后端接口/待产品确认是否要补导出”推进。

### 二、确认“非后端缺失”的项（接口已有数据或页面已有能力）
- `入库记录` 新增列 `入库库房`：接口 `GET /dev-api/system/inventoryStatistics/inStatistics` 已返回字段 `storeHouseName`。
  - 结论：非后端缺失，前端可直接接字段展示。
- `出库记录` 新增列/搜索 `出库库房`：接口 `GET /dev-api/system/inventoryStatistics/outStatistics` 已返回字段 `storeHouseName`。
  - 结论：非后端缺失，前端可直接接字段展示与筛选参数联调。
- `出库记录` 的“客户/库房列放到车号前”属于纯前端列顺序调整，无后端阻塞。
- `油卡消费充值明细表`：当前页面无导出按钮，且未看到导出接口调用。
  - 结论：更像“历史未实现/未接入”，暂不支持判定为回归缺陷。
- `现金加油台账`：当前页面无导出按钮，且未看到导出接口调用。
  - 结论：更像“历史未实现/未接入”，暂不支持判定为回归缺陷。
- `运费科目明细账` 列名当前仍为 `司机名称`（不是 `车牌号`）。
  - 结论：这是前端展示命名需求，非后端缺失。
- `运费科目明细账` 在选择车牌后，接口 `GET /dev-api/statistics/freightDetail?...` 返回 `otherAccountsName`、`otherBankNo` 等字段且有值。
  - 结论：“收款户名账号为空”并非后端普遍缺失，更可能是未筛选导致无数据或页面字段口径不一致。

### 三、补充说明（便于后续开发排期）
- `运费当日业务发生统计` 这个页面为cashOilRecharge.vue，导出接口为 /statistics/export/todayorderfreightsummary，可以参考其他页面的导出写法写一下。