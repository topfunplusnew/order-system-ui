# 2026-03-29 功能概述 — 实施与验收说明

本文档对应 `todo/2026-03-27:功能概述.md` 中的需求，说明本次在前端仓库内**已修改的文件**、**行为变化**及**验收口径**（含「已实现 / 待后端 / 待联调」）。

---

## 1. 入库记录 / 出库记录

| 验收项 | 状态 | 说明 |
|--------|------|------|
| 入库记录新增列「入库库房」 | **已实现** | 表格列增加 `storeHouseName`，与接口文档中字段名一致；搜索区增加「入库库房」输入框，查询参数沿用 `storeHouseName`。 |
| 出库记录「客户/库房」在「车号」前 | **已实现** | 调整 `columns` 顺序为：出库日期 → 客户/库房 → 车号 → 出库库房 → … |
| 出库记录新增列「出库库房」、搜索「出库库房」 | **已实现** | 列 `storeHouseName`；搜索项绑定 `storeHouseName`，请求中已传参。 |

**涉及文件**

- `packages/order-system/src/views/inventoryDetailViews/inventoryIn/index.vue`
- `packages/order-system/src/views/inventoryDetailViews/inventoryOut/index.vue`

---

## 2. 导出按钮（资金变动详情 / 资金总额 / 本日应当利润）

| 页面 | 状态 | 接口路径（与需求文档一致，`download` POST） | 参数摘要 |
|------|------|---------------------------------------------|----------|
| 资金变动详情 | **已实现 + 待联调** | `statistics/export/getAllMoneyChange` | `startTime`、`endTime`（取自当前日期范围）、`targetDate` |
| 资金总额 | **已实现 + 待联调** | `statistics/export/getAllMoney` | `endTime`（当前所选日期） |
| 本日应当利润 | **已实现 + 待联调** | `statistics/export/getDailyProfitStatistics` | `date`（当前所选日期） |

**涉及文件**

- `packages/order-system/src/views/system/moneyChange/index.vue` — 搜索区旁增加「导出」。
- `packages/order-system/src/views/system/Statement/totalMoneyChange.vue` — 搜索区旁增加「导出」。
- `packages/order-system/src/views/system/Statement/DailyProfitStatistics.vue` — 搜索区旁增加「导出」。

**说明：** 本日应当利润导出路径按与查询接口 `getDailyProfitStatistics` 对应的常见命名约定编写；若后端实际路径或参数名不同，需联调后改一处即可。

---

## 3. 油卡消费充值明细表 / 现金加油台账 — 导出

| 页面 | 状态 | 接口路径 | 结论（对应原需求「回归 / 未实现」） |
|------|------|----------|-------------------------------------|
| 油卡消费充值明细表 | **已实现 + 待联调** | `system/oilCard/exportOilCardBalanceDetail` | 原页面无导出、无调用，属**历史未接入**；本次已补按钮与 `download`。 |
| 现金加油台账 | **已实现 + 待联调** | `system/carApply/exportCashRefuelingLedger` | 同上，**历史未接入**；本次已补。 |

**涉及文件**

- `packages/order-system/src/views/system/Statement/OilCardBalanceDetail.vue`
- `packages/order-system/src/views/system/Statement/cashOilRecharge.vue`

---

## 4. 运费当日业务发生统计（页面：`systemFreightChangeSummary.vue`）

**说明：** 需求文档中误写为 `cashOilRecharge.vue`；菜单「运费当日发生业务统计」实际对应 `system/Statement/systemFreightChangeSummary.vue`。该页**原本已有**导出（`statistics/export/todayorderfreightsummary`），本次未重复添加。

| 验收项 | 状态 | 说明 |
|--------|------|------|
| 「车队」列无数据 | **已实现（前端兼容）** | 列改为优先显示 `fleet`，若无则尝试 `fleetName`、`motorcadeName`，避免后端字段名不一致导致空白。**若三者皆空，仍为后端未返回。** |
| 导出功能缺失 | **待确认** | 页面已有导出入口；若现场仍认为缺失，请核对权限与后端是否返回文件流。 |

**涉及文件**

- `packages/order-system/src/views/system/Statement/systemFreightChangeSummary.vue`

---

## 5. 运费科目明细账（`FreightDetail.vue`）

| 验收项 | 状态 | 说明 |
|--------|------|------|
| 列名「司机名称」→「车牌号」 | **已实现** | 展示优先 `carNo`，否则回退 `companyName`，避免无车牌字段时整行空白。 |
| 「收款户名账号」类展示为空 | **已实现（前端映射）** | 对方户名字段同时兼容 `otherAccountsName` 与历史拼写 `otherAcountsName`（与项目内付款/收款等模块一致）。**若仍为空，多为未选车牌或该行业务无对方户名数据。** |

**涉及文件**

- `packages/order-system/src/views/system/Statement/components/FreightDetail.vue`

---

## 6. 出差管理 — 出差报销明细表

| 验收项 | 状态 | 说明 |
|--------|------|------|
| 最左侧增加「申请时间」 | **已实现** | 使用字段 `fundsDate`；新增行时初始化 `fundsDate: ''`。若单行无 `fundsDate`，则回退展示主表 `form.payment.fundsDate`（付款申请通过后的资金日期）。 |

**涉及文件**

- `packages/order-system/src/views/system/businesstrip/index.vue`

---

## 7. 首页一键下载 — 文件名

| 验收项 | 状态 | 说明 |
|--------|------|------|
| 下载文件名格式 | **已实现** | 由所选日期生成：`{年}年{月}日一键下载数据留档.zip`（月份、日期为数字，无前导零亦可读）。 |

**涉及文件**

- `packages/order-system/src/views/index.vue` — 新增方法 `buildOneClickArchiveZipName`，`handleOption` 中 `downLoadOnce` 第三参数使用该文件名。

---

## 8. 汇总：交付状态一览

| 模块 | 前端 | 后端/联调 |
|------|------|-----------|
| 入库/出库列与筛选 | 已实现 | 已按文档使用 `storeHouseName`；若接口未返回，列为空 |
| 资金类三个导出 | 按钮已实现 | 待验证接口与权限 |
| 油卡/现金加油导出 | 按钮已实现 | 待验证接口与权限 |
| 运费当日「车队」 | 已做多字段兼容 | 仍无数据则属后端未返回 |
| 运费科目明细账列名/户名 | 已改 | 数据仍依赖查询条件与接口 |
| 出差报销申请时间 | 已加列 | `fundsDate` 需后端在明细中返回；否则依赖 `payment.fundsDate` |
| 首页一键下载文件名 | 已实现 | 无 |

---

**文档生成时间：** 2026-03-29（以仓库本次提交为准，验收时请对照 git diff）。
