1、加油卡消费信息，弹窗加上三行数据：取卡时间、还卡时间、申请人 在使用加油卡表单项下面
  新增加油卡消费信息
  POST /system/OilCardConsume
  接口ID：389050204
  接口地址：https://app.apifox.com/link/project/4903004/apis/api-389050204
  多了三个字段，apifox的新增和修改修改了样例，可以参考最后三个字段的名称和取值



2、现金加油台账加一列“附件”，引用“车辆派出管理”的附件 
   现在加油卡台账的表格数据中，也就是getList中会返回attachmentList，直接参考其他有附件的页面复刻逻辑即可。


3、支付日期、支付状态，中间加一个“审核人”。取付款申请审核的审核人
   逻辑：现在佣金的 paymentapply中额外返回auditinfo，取里面id最大的那个的元素的审核人 过滤掉"审核中"状态，按 id 取最大的审核人


4、 客户佣金、厂家佣金
    页面表格中每包片数、包数，去掉，加上出厂片数、出厂单价、采购含税、杂费、出厂货款
  这个返回了新的四个字段
    /**
     * 出厂片数
     */
    @BigDecimalMaxDigits
    @TableField(exist = false)
    private BigDecimal pieces;

    /**
     * 出厂单价
     */
    @BigDecimalMaxDigits
    @TableField(exist = false)
    private BigDecimal paymentFactory;

    /**
     * 采购含税（0=否,1=是）
     */
    @TableField(exist = false)
    private Long isIncludeTaxFactory;

    /**
     * 出厂货款
     */
    @BigDecimalMaxDigits
    @TableField(exist = false)
    private BigDecimal paymentsWithSundry;
 因为杂费已经返回了


