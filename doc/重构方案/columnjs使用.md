column.js 是对页面中的表格列和表单项中的列进行定义

includeInForm 如果是 true 的话，代表这个列不仅表格中有，表单中也会需要填写这一列 如果配置为 false,那么就只有表格中会展示这个列，表单中不会展示

visible 是表格列的显隐性，如果配置为 false，那么就是不展示，true 为展示，和显隐列组件可以联动（RightToolBar/index.vue）
